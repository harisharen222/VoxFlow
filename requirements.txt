# FastAPI web framework and ASGI server
fastapi==0.115.0
uvicorn[standard]==0.32.0

# Pydantic for request/response models
pydantic==2.9.2

# Environment variable loading
python-dotenv==1.0.1

# Qdrant vector database client
qdrant-client==1.12.0

# Removed sentence-transformers to stay under Lambda 500MB limit

# HTTP client (used for URL ingestion + later phases: Vapi, Claude)
httpx==0.27.2

# Phase 2: ingestion pipeline
pymupdf==1.24.10          # PDF text extraction (fitz)
beautifulsoup4==4.12.3    # HTML parsing for URL ingestion
nltk==3.9.1               # Sentence tokenizer for chunker.py

# Phase 4: LLM (RAG) — Google Gemini
google-generativeai==0.8.3  # Gemini 2.5 Flash for RAG + fact extraction

# Phase 6: Connectors (Google Drive + Notion)
google-api-python-client==2.149.0
google-auth==2.35.0
notion-client==2.2.1
